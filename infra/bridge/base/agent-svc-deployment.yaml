#! agent-svc-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: agent-svc
    namespace: infra
    labels:
        com.docker.compose.project: infra
        com.docker.compose.service: agent-svc
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: infra
            com.docker.compose.service: agent-svc
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: infra
                com.docker.compose.service: agent-svc
                com.docker.compose.network.default: "true"
        spec:
            containers:
                - name: agent-svc
                  image: infra-agent-svc
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: ALLOW_UNGROUNDED_ANSWERS
                      value: "${ALLOW_UNGROUNDED_ANSWERS}"
                    - name: CHAT_MODEL
                      value: "${CHAT_MODEL}"
                    - name: COMMS_URL
                      value: "${COMMS_URL}"
                    - name: DB_HOST
                      value: "${DB_HOST}"
                    - name: RETRIEVAL_URL
                      value: "${RETRIEVAL_URL}"
                  ports:
                    - name: agent-svc-8082
                      containerPort: 8082
