#! web-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: web
    namespace: infra
    labels:
        com.docker.compose.project: infra
        com.docker.compose.service: web
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: infra
            com.docker.compose.service: web
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: infra
                com.docker.compose.service: web
                com.docker.compose.network.default: "true"
        spec:
            containers:
                - name: web
                  image: infra-web
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: NEXT_PUBLIC_GATEWAY_URL
                      value: "${NEXT_PUBLIC_GATEWAY_URL}"
                  ports:
                    - name: web-3000
                      containerPort: 3000
