#! retrieval-svc-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: retrieval-svc
    namespace: infra
    labels:
        com.docker.compose.project: infra
        com.docker.compose.service: retrieval-svc
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: infra
            com.docker.compose.service: retrieval-svc
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: infra
                com.docker.compose.service: retrieval-svc
                com.docker.compose.network.default: "true"
        spec:
            containers:
                - name: retrieval-svc
                  image: infra-retrieval-svc
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: DB_HOST
                      value: "${DB_HOST}"
                    - name: DB_NAME
                      value: "${DB_NAME}"
                    - name: DB_PASS
                      value: "${DB_PASS}"
                    - name: DB_PORT
                      value: "${DB_PORT}"
                    - name: DB_USER
                      value: "${DB_USER}"
                    - name: EMBEDDINGS_MODEL
                      value: "${EMBEDDINGS_MODEL}"
                    - name: OPENAI_API_KEY
                      value: "${OPENAI_API_KEY}"
                  ports:
                    - name: retrieval-8081
                      containerPort: 8081
